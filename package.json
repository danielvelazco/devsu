{
  "name": "devsu",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "test:api": "cypress run --spec ./cypress/tests/api/*.js",
    "test:e2e": "cypress run --spec ./cypress/tests/e2e/*.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.0"
  }
}
